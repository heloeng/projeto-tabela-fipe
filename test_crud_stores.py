from crud_stores import create_store
from crud_stores import update_store
from crud_stores import delete_store
from crud_stores import get_all_stores
from crud_stores import get_store_by_id

#testar manualmente no vscode: uma de cada vez

#create_store("Loja Sul", "Curitiba", "PR", "Rua XV de Novembro", "Centro", "789", "80020-310")
#get_all_stores()

# Buscar uma loja específica pelo ID - Substitua pelo ID desejado
#get_store_by_id(4) 

#update_store(2, name="Loja Centro Reformada", city="São Paulo")
#delete_store(2)

#terminal do shell
# SELECT * FROM stores_table;
